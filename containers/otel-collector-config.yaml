receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  logging:
    logLevel: info

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging]
